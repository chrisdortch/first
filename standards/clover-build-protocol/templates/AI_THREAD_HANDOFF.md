# AI Thread Handoff — Clover Build Protocol

Use this at the beginning of a future software-building thread.

---

Read the canonical Clover Build Protocol in `chrisdortch/first`:

1. `AI_START_HERE.md`
2. `CLOVER_BUILD_PROTOCOL_POINTER.json`
3. `standards/clover-build-protocol/CURRENT.md`
4. the referenced numbered specification;
5. `standards/clover-build-protocol/registry/projects.json`.

Then inspect the exact target project's current repository and deployment. Do not rely on the registry alone when a fact may have changed.

Apply the deterministic-first, AI-on-escalation, owner-controlled-release model. Conserve model tokens by using fixed tests and compact evidence for repeatable facts. Use AI judgment for novel failures, architecture, visual quality, product meaning, authenticated owner-only flows, and final acceptance.

Do not treat the protocol or a passing test as authorization to merge, deploy to production, change a domain, alter secrets, mutate production data, purchase anything, send external messages, or expand public access. Obtain current project-specific owner authorization for the exact irreversible action.

If the current task changes the protocol itself, create a new version and preserve all prior versions.

---
