# Implementation record — 1.2.0 candidate

Prepared: 2026-08-19 America/Chicago

Candidate status: `unvalidated`

Implemented additively:

- versioned 1.2 schemas and templates;
- central enrollment binding for policy SHA-256 and immutable source anchors;
- schema-first reusable workflow ordering;
- non-shell structured command runtime;
- tracked-tree, source-commit, committed-tree, and policy snapshots;
- post-command and post-browser invariant verification;
- evidence-based side-effect observations;
- candidate integrity and regression tests;
- a SHA-256 manifest preserving every selected 1.1.0 and earlier runtime artifact byte.

Not claimed:

- exact-head GitHub Actions validation;
- project enrollment;
- promotion to current;
- merge or deployment authority;
- provider-side effect absence.
