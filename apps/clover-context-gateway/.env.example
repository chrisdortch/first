PORT=8787
# Defaults to the repository root when the app lives at apps/clover-context-gateway.
# Override when the server is deployed from a copied context bundle.
CONTEXT_ROOT=
CONTEXT_SOURCE_REF=main
PUBLIC_BASE_URL=http://localhost:8787

# Optional future read-only live adapters. Never commit values.
GITHUB_TOKEN=
VERCEL_TOKEN=
VERCEL_TEAM_ID=
