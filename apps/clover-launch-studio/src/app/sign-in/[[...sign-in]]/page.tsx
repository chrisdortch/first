export default function SignInPage() {
  return (
    <main className="auth-page">
      <section className="auth-card" aria-labelledby="sign-in-title">
        <p className="eyebrow">Private owner boundary</p>
        <h1 id="sign-in-title">Sign-in adapter not configured</h1>
        <p>
          Launch Studio accepts authority only from a server-verified immutable provider subject.
          Clerk is a candidate provider, not selected or installed. Production access therefore fails closed.
        </p>
        <p>No email address, display name, URL parameter, widget identity, or ChatGPT memory grants access.</p>
      </section>
    </main>
  );
}
