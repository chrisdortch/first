# ChatGPT Personal Pro — Clover Core Trunk Activation Review

Use GitHub, Vercel, and the exact Clover Context Gateway candidate. Work
read-only. Do not merge, deploy or promote production, change aliases, domains,
environments, secrets, access policies, or permissions, access private or
production data, send a message, exercise a payment, make a purchase, or spend
money.

## Review target

Repository: `chrisdortch/first`

Candidate branch:
`platform/clover-core-trunk-activation-v0.1-20260820`

Required parent and stacked base:
`platform/clover-core-trust-slice-v0.2-20260818` at
`364a9a96829f323aa00a679804fdd7ed879043b5`

Refresh the candidate branch and draft pull request before reviewing. Treat
the branch's current exact head, CI artifacts, and target-null Gateway 0.3.1
preview as unknown until independently read back. Do not substitute PR #16,
Gateway 0.3.0, canonical `main`, or conversational memory for this candidate.

## Required durable records

Read, schema-validate, and hash-verify at least:

- `portfolio/status/candidates/2026-08-20/status.json`
- `portfolio/status/candidates/2026-08-20/priority-input.json`
- `portfolio/status/candidates/2026-08-20/priority-output.json`
- `portfolio/core/today/2026-08-20/session.json`
- `portfolio/core/handoff/index.json`
- every capsule, Action Envelope, receipt, and review pointer reached from that
  Handoff index
- `CLOVER_OWNER_START.md`
- `CHATGPT_PROJECT_INSTRUCTIONS.md`
- `CODEX_CLOVER_OPERATOR.md`
- `CLOVER_CONNECTOR_ROUTING.md`
- `CLOVER_HANDOFF_LEDGER.md`
- `portfolio/core/CORE_TRUNK_ACTIVATION_IMPLEMENTATION_RECORD_V0.1.md`

## Independent checks

1. Confirm PRs #15 and #16 remain open, draft, unmerged, and at the exact
   preserved heads. Confirm the new candidate is stacked directly on PR #16.
2. Recompute the protected August 17 status, Registry 1.0, Registry 2.0,
   Constitution 0.1, ratification receipt, and legacy ledger hashes. Reject any
   historical rewrite.
3. Confirm the August 20 status keeps broad-mission, owner-usable,
   live-production, and verified-candidate percentages separate, with explicit
   date, scope, weights, confidence, freshness, exclusions, and lifecycle.
4. Regenerate the priority output from all 19 Registry 1.0 P0/P1 inputs.
   Confirm unknown factors remain unranked, one Core item is active, no
   affiliated branch is falsely active, the top three are exactly supported by
   the stored evidence, and the recommended affiliated precursor remains
   proposal-only. Confirm that no implementation branch is claimed while the
   exact repository source and rollback remain unknown.
5. Confirm every Branch Capsule is a minimized persistent Cell record and
   contains no raw legal, financial, health, customer, guest, staff,
   credential, message, payment, reservation, or transactional data.
6. Confirm each Handoff record is closed-schema and self-hashed; Action IDs are
   unique; approval, lifecycle, expiry, single-use, source, target, operations,
   paths, connectors, prohibitions, evidence, readback, cost, rollback, and
   receipt bindings cannot be widened or substituted. Handoff metadata must not
   bypass the hardened runtime Action Envelope 0.2 when a future mutation is
   involved.
7. Confirm the completed demonstration Action ID represents only the actual
   public read-only source refresh, and the first affiliated pre-branch Action
   ID remains proposed, unapproved, unexecuted, and without a fabricated
   receipt or review. It must not be described as an implementation-branch
   Action Envelope.
8. Confirm Gateway 0.3.1 preserves 0.3.0 separately, keeps canonical v1 status
   current, loads candidate records only as source-bound optional siblings,
   fails missing or corrupt records closed, exposes exactly four read-only MCP
   tools, and never places Today inside Command Packet 1.2.
9. Read back the exact-head GitHub Actions runs and artifacts. Verify artifact
   digests and that every claimed source/test result binds the candidate head.
10. Read back the Gateway preview by immutable deployment ID and URL. Confirm
    `READY`, `target: null`, exact source, no manual alias, no production
    promotion, and read-only/default-deny authority. Check desktop and mobile
    rendering, console errors, page errors, failed requests, and horizontal
    overflow only if current evidence actually exists.
11. Confirm CloverApps live 7.4.2, Sites 80/81, Serenity PR #22, and RollinD
    main/PR #3/PR #6 remain unchanged. Do not open any private Cell records.
12. Distinguish explicit purchase or money movement (`USD 0`) from unknown
    provider-metered usage cost. Do not infer zero marginal billing.

## Return

Return exactly one decision: `APPROVE`, `AMEND`, or `HOLD`, bound to the exact
candidate commit and the exact completed execution-receipt hash. List source
facts, inferences, failed or unavailable checks, authority still required,
rollback anchors, and every remaining unknown. If approved, approval means only
that this draft candidate is coherent enough for its next separately authorized
gate; it does not authorize merge or production promotion.

If a review decision is later committed, create a new closed-schema Handoff
Review Decision bound to the exact receipt. Do not rewrite the receipt or any
prior review.
