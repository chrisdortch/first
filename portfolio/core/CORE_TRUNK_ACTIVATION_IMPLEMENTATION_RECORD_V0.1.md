# Clover Core Trunk Activation — implementation record 0.1

Status: `candidate-unmerged-undeployed`
Date: 2026-08-20
Rollback parent: `364a9a96829f323aa00a679804fdd7ed879043b5`
Candidate branch: `platform/clover-core-trunk-activation-v0.1-20260820`

This additive candidate implements the Minimum Useful Core owner loop without changing PR #15, PR #16, CloverApps Sites 80/81, Serenity, RollinD, or a production system.

## Outcome

The candidate can present one source-bound Clover Today session with:

- current, candidate, live, historical, and unknown state kept distinct;
- four separately scoped completion metrics;
- a transparent 19-target P0/P1 priority assessment;
- three headline priorities, including blocked high-risk work;
- one eligible proposed next affiliated action, limited to a read-only identity-reconciliation precursor until an exact branch and rollback are known;
- a minimized Branch Capsule set;
- one stable Action ID that resolves to one immutable envelope; and
- a read-only Gateway surface that cannot execute, merge, deploy, communicate, spend, or grant itself authority.

The Handoff Ledger includes a completed sanitized read-only demonstration and a separate unapproved proposed action. It does not convert a proposal into approval. A future mutating action must also satisfy the hardened Core Action Envelope v0.2 and every applicable protocol.

## Source model

Canonical v1 status and Registry 1.0 remain current. The August 17 status and its 41% broad-mission estimate remain historical. Registry 2.0, the August 20 status, Priority Model 0.1, Handoff Ledger 0.1, Today Session 0.1, and Gateway 0.3.1 remain candidates.

Raw legal, financial, health, customer, guest, staff, credential, email, message, payment, reservation, and transactional data remain in sovereign Cells. The public Core candidate contains only minimized public operating metadata and explicit unknowns.

## Version and deployment boundary

Gateway 0.3.0 remains the preserved PR #16 candidate and exact preview baseline. Because this candidate changes Gateway bytes, the additive source identity is 0.3.1. A successful non-production preview, if later created under this gate, must be bound to the exact container commit, report `target: null`, have no manually assigned alias, keep write tools disabled, and report standing production authority as false.

This file intentionally does not claim the Git commit that contains itself. Git history, exact-head CI, and the external source-bound receipt bind the final container commit without a circular self-reference.

## Preserved authority boundary

The candidate grants no standing write or production authority. It performs no merge, production deployment or promotion, private-data access, secret reveal or mutation, external communication, purchase, payment, domain or alias change, permission or access-policy change, persistent environment change, or OpenAI Site mutation.

## Promotion gates

Before any later merge decision, require:

1. exact-head schema, unit, privacy, disclosure, hash, and source-binding checks;
2. exact-head Gateway and Core CI on supported Node versions;
3. a source-bound target-null Gateway 0.3.1 preview with desktop/mobile/console readback;
4. independent review of the exact receipt; and
5. a separate owner approval naming the exact merge or production action.
